import tkinter

window = tkinter.Tk()
window.title("Directory Organizer")
window.minsize(width=600, height=200)


# Label

my_label = tkinter.Label(text="Please enter the full path of the directory to organize.", font=("Arial", 20, "bold"))
my_label.pack(expand=True)















window.mainloop()